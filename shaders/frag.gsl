push {
  uvec2 _ScreenDims;
  uint  _TraceOutputIndex;
  uint  _SamplerIndex;
};

in vec2 _UV;

out vec4 _FragColor;

void main() {
  vec2 uv = 0.5 * (_UV + 1.0);
  vec3 color = texture(sampler2D(grf_Tex2D[_TraceOutputIndex], grf_Sampler[_SamplerIndex]), uv).rgb;
  _FragColor = vec4(color, 1.0);
}
